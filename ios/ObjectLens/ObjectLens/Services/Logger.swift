import OSLog
extension Logger { static let camera = Logger(subsystem: "ObjectLens", category: "Camera"); static let inference = Logger(subsystem: "ObjectLens", category: "Inference"); static let model = Logger(subsystem: "ObjectLens", category: "Model"); static let persistence = Logger(subsystem: "ObjectLens", category: "Persistence"); static let grouping = Logger(subsystem: "ObjectLens", category: "Grouping"); static let ui = Logger(subsystem: "ObjectLens", category: "UI"); static let permissions = Logger(subsystem: "ObjectLens", category: "Permissions"); static let performance = Logger(subsystem: "ObjectLens", category: "Performance") }
